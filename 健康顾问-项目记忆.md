# 健康顾问｜项目记忆

> 本文件是"健康顾问"项目（`D:\AIWorkspace\projects\health-advisor-internal`）的长期、可跨会话读取的本地记忆入口。

更新日期：2026-09-04（Asia/Shanghai）

## 1. 用户的固定要求（训练建议）

1. **动作必须有权威出处**：训练动作只采用权威机构（如 AAOS 美国骨科医师学会、Mayo Clinic 梅奥诊所、ACSM 美国运动医学会等）推荐的方案，并在输出中标注出处；不接受无来源的自编动作组合。
2. **每个动作必须带详细解释**：用户是健身新手，每个动作要写清具体怎么做、发力部位、要领和常见错误（例如"手臂画圈"要解释以肩为轴画圈，"死虫式"要解释起始姿势）。
3. **不伤身体，尤其膝盖压力小**：优先低冲击、膝盖不负重不屈曲或浅幅度的动作；膝盖是铰链关节，不做膝环绕类旋转动作；深蹲类一律浅蹲、扶椅、重心在脚跟。
4. **疼痛即停**：沿用 AAOS 原则——运动中不应感到疼痛，任何动作引起疼痛立即停止；有确诊伤病先由医生确认动作。

## 2. 已完成：恢复日模板权威化（2026-07-20）

- `app/analytics.py` 的 `_WORKOUT_LIBRARY["bodyweight"]["recovery"]` 已替换为"低冲击护膝恢复训练"：仰卧直腿抬高、臀桥、侧卧髋外展、扶椅半蹲、站姿腘绳肌弯举、提踵、跪姿平板支撑、鸟狗式 + 三个 AAOS 拉伸。
- 每个动作带 `guide`（详细做法+要领）和 `source`（AAOS / Mayo Clinic）字段；模板带 `sources` 出处列表；`_build_workout` 返回值和 `app/templates/index.html` 已渲染这三项。
- 替换要点：删膝环绕；靠墙静蹲 → 仰卧直腿抬高 + 扶椅半蹲；标准平板 → Mayo 跪姿版；死虫式 → 鸟狗式（Mayo 官方核心库）。
- 42 个测试通过，服务已重启并验证线上生效。

## 3. 已完成：全部训练模板权威化（2026-07-26 收尾）

`_WORKOUT_LIBRARY` 全部模板均已迁移为"guide 详解 + source 出处 + 模板级 sources 列表"格式（recovery 于 2026-07-20 完成，其余六个于 2026-07-26 完成，详见 §10 当日日志）。新增 tests/test_workout_authority.py 作为长期门禁：每个动作必须带 ≥60 字 guide 和 AAOS/Mayo/ACSM 出处、每个模板必须带 sources、处方字段（name/detail/note）禁止出现膝绕环类动作、swim/run/ride 时长处方不得擅改。今后改动任何模板必须通过该测试。

## 4. 服务运维备注（2026-07-20 改为"非常驻"模式）

- 本地服务：`http://127.0.0.1:8000`，uvicorn 无热重载，改代码后需重启生效。
- **绑定方式（2026-07-26 起）**：uvicorn 绑 `0.0.0.0`（手机局域网可达），`app/main.py` 的 `lan_token_guard` 中间件要求**非回环来源**必须带访问令牌（`?token=…` 查询参数或 `X-LAN-Token` 头，否则 403）；**回环（127.0.0.1/::1）免令牌**，daily_sync.py、open_dashboard.py 等本地脚本不受影响。`/static/` 静态资源放行（只是界面文件，无健康数据），页面与 API 全部保护。令牌来源：环境变量 `HEALTH_LAN_TOKEN`；缺省自动生成并持久化在 `data/lan_token.txt`（data/ 已 gitignore，重启不变，手机书签持续有效）。`dashboard.js` 与 `mobile.html` 自动把 URL 里的 token 拼到所有 /api 请求（含心跳）和站内链接。防火墙：本机已有"Python"公用配置文件 TCP 入站允许规则（WLAN 即公用），无需新增；如换机器不通，管理员执行 `netsh advfirewall firewall add rule name="Health Assistant 8000" dir=in action=allow protocol=TCP localport=8000`。
- **用户要求后端不常驻**：登录自启动任务 `HealthAssistant` 已于 2026-07-20 禁用（`Disable-ScheduledTask`，恢复用 `Enable-ScheduledTask -TaskName HealthAssistant`）。现在只有两种运行时机：①每日同步任务临时拉起、同步完自动停止；②用户点书签手动启动，**关闭看板标签页后约 5 分钟无心跳自动退出**（心跳机制见下）。
- 手动入口：浏览器书签 `healthboard://`（2026-07-20 注册的自定义协议，注册表 `HKCU\Software\Classes\healthboard` → `wscript.exe scripts\run_healthboard.vbs` → 隐藏运行 `scripts\healthboard.bat`，删除该键即卸载协议）。桌面 bat 已按用户要求删除。**全程无任何命令行窗口**。
- **2026-09-04 修复实录**：项目改名 `health_assistant` → `health-advisor-internal` 后，注册表 `shell\open\command` 仍指向旧绝对路径 `...\health_assistant\scripts\run_healthboard.vbs`（目录已不存在），导致 healthboard:// 完全失效。已 `reg add` 改为指向 `...\health-advisor-internal\scripts\run_healthboard.vbs`，模拟触发验证 HTTP 200。**教训：项目目录改名/迁移后必须同步检查注册表协议键、计划任务、快捷方式里的绝对路径。**
- 心跳自动退出（2026-07-20）：`dashboard.js` 每 30 秒 POST `/api/heartbeat`；`app/main.py` 的 `_idle_watchdog` 每 30 秒检查，超过 `HEALTH_IDLE_TIMEOUT_SECONDS`（默认 300 秒）无心跳就 `os._exit(0)`。每日同步一两分钟内结束并自行停止，不受看门狗影响。
- `scripts\healthboard.bat` 逻辑：`pythonw scripts\open_dashboard.py`（等端口就绪 → **先采集小米 + Strava 最新数据 → 采完才打开看板页**，日志在 `logs/open_dashboard.log`），若 8000 已在监听则只同步+开页面、不重复启动 uvicorn（此时点书签等于一次全量手动刷新）；uvicorn 输出追加到 `logs/app.out.log` / `app.err.log`。**注意：bat 内容必须纯 ASCII**——UTF-8 中文 title 行会被 cmd 按 GBK 断行解析，吞掉下一行 `cd /d`（2026-07-20 实录，协议唤起时工作目录错误导致 uvicorn 起不来）；`open_dashboard.py` 的 print 必须带 try/except（pythonw 无控制台）。
- 旧进程可能是 multiprocessing 子进程（spawn_main），杀父进程后要确认子进程退出、8000 端口确实释放，再启动新进程；`daily_sync.py` 的 `stop_service()` 用 `taskkill /T` 连带杀子进程。

## 5. 2026-07-20 代码审查 10 项 bug 修复

- 修复位置：D:\AIWorkspace\projects\health-advisor-internal（app/analytics.py、app/main.py、app/static/js/dashboard.js）。
- 要点：_build_workout 休息/恢复日 intensity 统一为"恢复"；save_quick_meal 对 naive eaten_at 按本地时区入库；_iso_to_local_date 真正转本地时区；_body_analysis 周口径统一系统本地时区；豆腐鸡蛋 variant density 0.10、minimum 300；latest_steps 为 NULL 时返回 None；饮食调整分支 -0.15 边界消除区间空洞并按 trend_sufficient 区分文案；dashboard.js 用餐提醒用本地日期；Strava OAuth/同步捕获 httpx.HTTPError 转 502。
- 回归测试：tests/test_bugfix_regressions.py（问题1/2/5 共4条）；test_low_sleep_reduces_intensity 断言随修复由"低强度耐力"改为"恢复"。pytest 46 passed。

## 6. 每日自动同步（用户要求：数据必须每天自动同步）

- 用户明确要求 Strava 和小米数据每天自动同步，不接受手动刷新。
- 实现：`scripts/daily_sync.py` 通过本地服务端点（`/api/sync/xiaomi`、`/api/sync/strava`）同步，服务不在时先拉起、**同步完后自动停止**（2026-07-20 起，配合"后端不常驻"要求）；日志写 `logs/daily_sync.log`，有失败项退出码为 1。
- 部署：Windows 计划任务 `HealthAssistantDailySync`，每天 07:30（收睡眠+晨重）和 21:40（收白天活动和步数）运行，`StartWhenAvailable` 补跑错过的任务；安装/卸载用 `scripts/install_sync_task.ps1`。
- 任务审计 XML 存于 `D:\AIWorkspace\workspace_admin\scheduled-task-audits\HealthAssistantDailySync-{before,after}.*`。
- 小米 passToken：无官方有效期，社区经验几个月到一年；上次扫码 2026-07-14，截至 2026-07-20 每日同步均成功。**注意**：2026-07-20 前 `xiaomi_sync.py` 把任何连接失败（含网络波动）都误报为"passToken 可能已过期"；现已修复——连接失败重试 3 次，并按 `adapter.last_error` 区分：网络类错误提示"无需重新扫码"，只有鉴权类错误才提示重新 `python scripts/mijia_health_sync.py login` 扫码。另外小米云偶发采集中途失败（空原因的 "Mi Fitness request failed: "，2026-07-20 实录，重试即成功），`_collect_all` 已加整轮重试。Strava webhook/ngrok 当前按配置关闭（HEALTH_ENABLE_NGROK 未开启），实时推送停用，但每日两次轮询同步不受影响。
- **正式重试（2026-07-26，针对 07-23/24/25 连续三晚 21:40 场 TimeoutError）**：①`xiaomi_sync.py` 连接与采集重试改为指数退避（3s、6s），鉴权类错误（401/Unauthorized/passToken 等标记）不再重试、直接提示重新扫码；②`scripts/daily_sync.py` 对网络类失败（TimeoutError/URLError/OSError）指数退避重试 2 次（30s、60s），HTTP/鉴权类错误不重试；③`/api/sync/xiaomi` 端点加 asyncio 锁串行化，客户端超时重试时不会与仍在执行的上一轮并发写库（upsert 幂等）。测试：tests/test_lan_access.py。

## 7. 文本编码规则（2026-07-20）

- 文本编码按全局 §3.4（文本统一 UTF-8）执行，本项目具体约定：Python 读写文本文件显式 `encoding="utf-8"`（日志、文本导出含中文）；健康数据存 SQLite，原生存文本无需额外处理。
- PowerShell 5.1 脚本（`run.ps1`、`scripts/*.ps1`）显式 `-Encoding UTF8`，`.ps1` 必须保存为 UTF-8 带 BOM——无 BOM 时 PS 5.1 按 GBK 解析中文导致脚本断裂（2026-07-20 实录）。
- 与 `projects/health-advisor-internal/AGENTS.md` 的 "Text encoding" 小节保持一致。

## 8. 外部升级建议的评审规则（2026-07-20 建立）

闭环契约见 `workspace_admin\docs\UPGRADE_SUGGESTION_LOOP.md`（调度持有）；本项目目录名 `health_advisor`，建议与反馈文件在 `output\upgrade_suggestions\health_advisor\`。

### 8.1 建议分类与验证手段

| 类别 | 例子 | 验证手段 |
|---|---|---|
| 训练内容/模板 | 新动作、模板调整、饮食建议 | 权威出处核验（AAOS/Mayo/ACSM 等，§1）+ 不伤膝/疼痛即停原则逐条核对 + pytest 回归 |
| 数据/同步 | 新数据源、同步逻辑、指标口径 | 同步成功率与数据完整性核验；不破坏"每日自动同步"（§6） |
| 工具链/看板 | 看板功能、脚本、心跳机制 | pytest 通过；回环免令牌 + 局域网令牌访问（§4）；非常驻模式（§4 心跳看门狗）不回归 |
| 运维 | 计划任务、部署方式 | 可回滚；`StartWhenAvailable` 补跑；任务审计 XML 留档惯例（§6） |

### 8.2 判定门槛（任一不过即拒绝或暂缓）

1. **隐私红线（一票否决）**：原始健康数据（SQLite、完整导出、原始照片）零外发、零入 git；仅在用户知情授权的功能内，按脱敏+最小化原则外发必要内容（当前实例：餐食照片→KI 中转站图片分析（gpt-5.6-sol，直连）、健康数据摘要→云端 Kimi 助手）。
2. **医学安全性（一票否决）**：无权威出处的训练/健康建议直接 REJECT；违反不伤膝、疼痛即停原则的直接 REJECT。
3. **可验证**：有 §8.1 对应类别的可执行验证手段。
4. **单变量**：能落实为一次只改一处的具体改动；笼统建议打回。
5. **兼容用户固定要求**：不得破坏 §1（出处+详解）、§4（后端非常驻）、§6（每日自动同步）。

### 8.3 判定档位与原因码

候选（CANDIDATE）/ 留存观察（HOLD，登记前置条件）/ 拒绝（REJECT，附原因码）：`VIOLATES_PRIVACY` / `NO_AUTHORITATIVE_SOURCE` / `UNSAFE_FOR_KNEE` / `UNTESTABLE` / `TOO_VAGUE` / `CONFLICTS_USER_REQUIREMENTS` / `DUPLICATE:<条目>` / `LOW_CREDIBILITY`。

### 8.4 目标声明与基线记分卡（2026-07-20）

**目标声明**（三层结构按契约 §3）：

- **结果目标（北极星）**：让用户在**零伤病**前提下身体更健康——体能、活动量、睡眠趋势长期向好。健康是慢变量、多因变量，任何"快"字头的优化（快速减重、突击训练）一律视为偏离北极星。
- **验收指标（通过线）**：

| 维度 | 目标（通过线） |
|---|---|
| 训练内容 | 100% 动作带权威出处（AAOS/Mayo/ACSM 等）与详细要领；零违反不伤膝/疼痛即停原则 |
| 数据同步 | 每日 07:30/21:40 两次自动同步成功率 100%；失败可自动恢复 |
| 工具链 | pytest 全通过；回环免令牌 + 局域网令牌访问（2026-07-26 起，§4） |
| 服务形态 | 非常驻模式不回归：无心跳 5 分钟自动退出，无残留进程 |
| 隐私 | 原始健康数据零外发、零入 git（一票否决项，不可 trade-off；知情授权功能内脱敏+最小化外发的口径见 §8.2） |

- **红线约束**：不伤膝/疼痛即停原则不可 trade-off；任何指标优化不得以牺牲医学安全性或隐私为代价。

**基线记分卡**：

| 维度 | 基线 | 改进怎么度量 |
|---|---|---|
| 数据同步 | 每日 07:30/21:40 两次自动同步，近期成功率 100% | 成功率、失败恢复时长（观察 2 周） |
| 内容权威化 | 全部 7 个模板（recovery + 六模板）已权威化（2026-07-26），test_workout_authority.py 长期门禁 | 权威化覆盖率 100%，门禁不回归 |
| 工具链 | pytest 72 passed（2026-07-27） | 门禁不回归 + 新能力有测试证据 |
| 服务形态 | 非常驻 + 心跳 5 分钟自动退出；书签手动唤起可用 | 无残留进程、唤起成功率 |

判定公式与回退义务按契约 §4：目标维度可度量改进 + 其他维度无回归；观察期内拿不出证据的升级回退。采纳后更新本表基线。

## 9. 已收尾：并行会话遗留文件已提交（2026-07-25）

2026-07-25 健康顾问会话按本节要求完成收尾：提交前核对 diff 与本文件 §4/§6 描述一致，pytest 49 passed，一次性提交（commit 97d7a51，7 个文件，含 `scripts/healthboard.bat`、`scripts/run_healthboard.vbs`）。此前正文（2026-07-20 晚调度会话检查结论）：pytest 49 passed；21:40 计划同步 xiaomi 报 `TimeoutError: timed out` 失败一次，21:47 重跑全部成功，非常驻模式正常。

## 10. 今日更新日志

### 2026-08-29（手环 APP/手机 APP 更新后通路检查，用户发问）

- 检查结论：**云→米桥→health.db 通路正常，断点在手环→手机→云上游**。当天 07:30 排定同步与 07:51 手动重跑均 exit 0（心率 4438、血氧 340、压力 665 条均入库）；但 07:30→07:51 两次拉取间云数据几乎无增量（仅 08-28 步数 42→81），说明手机没把新数据传上云。
- 数据现状：睡眠记录停在 08-24（此后 4 晚缺失）；步数 08-24 9926 → 08-25 8431 → 08-26 5703 → 08-27 796 → 08-28 仅 81，与 APP 更新时间吻合，疑更新后手环与手机未正常同步（或更新后未佩戴）。
- 已告知用户：手机端打开小米运动健康下拉强制同步、确认手环已连接（必要时重配对），同步后本会话再手动跑一次 daily_sync.py 验证补齐。后续若用户同步后云仍无数据，需排查 APP 更新是否改了上传口径。

### 2026-08-21（全面体检 bug 排查，工作区统一体检子任务）

- 工作树起始干净、无并行会话；基线 pytest 发现 1 个失败并修复，共整改 2 处（commit 4ff8f33）：
  1. `tests/test_nutrition_mobile.py` 的 `test_nutrition_summary_exposes_recent_logging_coverage` 是时间敏感 flaky 测试：夹具用墙钟 `now` 且未把 `now` 传给 `build_summary`，本地时间 00:00–02:00 运行时 `-1d` 与 `-1d-2h` 两条记录跨本地日期，`logged_days` 由 2 变 3 误报失败。改为固定时间（2026-07-16 08:00 UTC）并传 `now=now`（应用代码 `_nutrition_plan` 按本地日期计数是 07-20 修复的预期行为，非应用 bug）。
  2. `scripts/install_export_task.ps1` 含中文（`-Description` 行）但缺 UTF-8 BOM，违反 §7 编码约定；已补 BOM，PowerShell 5.1 `Parser::ParseFile` 验证通过（注意：无 BOM 时该文件在 PS 5.1 下也能解析，仅 Description 乱码，未造成断行）。
- 存疑未动（体检结论）：① `db.py save_photo_nutrition` 无 ON CONFLICT，同秒两张照片会 IntegrityError→500，但 eaten_at 含微秒，实际不可能触发；② `export_cloud_digest.py` 对 NULL total_kcal 会 TypeError，但当前库无 NULL 行且云端项目已取消（脚本不再排定）；③ `post_workout_recovery.py` raw_json 缺失时回退用 UTC start_date 判"今日"，raw_json 恒存在不会触发；④ `analytics.py` 第 1427 行 `item in recent` 用值相等判断，理论可误计，实际 synced_at 必不同。
- 整改后 pytest 77 passed，工作树干净。

### 2026-08-17（公开展示化，路线 B 落地）

- **用户拍板路线 B**：健康顾问对外展示走"全新干净公开仓"——`shkyyy18/health-advisor`（PUBLIC，首个提交 a0eb9ec，CI success），本地对应 `projects\health-advisor-public`；原私有仓改名 `health-advisor-internal`（本地 `projects\health-advisor-internal` origin 已同步，备份注册表同步改名）。**日常开发仍在 health-advisor-internal/internal 仓；对外更新=重新做脱敏快照推到 public 仓。**
- **L2 会审（Qwen+Codex 一致"整改后可公开"）后的两层整改**：① 工作仓真实修复（已推 internal）：启动日志不再打印带令牌 URL；隧道判定不再信可伪造的 x-forwarded-proto；MEAL_LLM 默认值清空不再硬编码中转站；README 如实写 0.0.0.0+令牌并改挂米桥链接。② 公开副本脱敏：删记忆文件（gitignore 防再跟踪）、hypervolt.html 去硬编码 MAC/内网 IP/膝伤个人化文案、内网 IP 全占位符、本机绝对路径全清、测试夹具体重数值平移脱敏。最终扫描零命中（主席抽查复核），77 测试在干净检出通过。
- **注意**：公开仓是快照非镜像——internal 的新功能要对外时，须重新走"archive→脱敏→扫描→推送"流程；记忆文件永不进公开仓。

### 2026-08-17

- **远端强推覆盖事件处置**：发现 GitHub `health-advisor` 远端 main 被人从归档旧树 `projects\health-advisor`（已定性只读归档、有自己独立 .git）force-push 覆盖，现行树 13 个提交（Hypervolt 全套 + 08-13 同步链路修复）未上岸。处置：本地建 `archive/remote-force-pushed-20260817` 分支保存被覆盖状态（其内容在归档树中本就在），经用户拍板后 `git push --force-with-lease` 恢复正统主线（ae6a81d...5175fcb forced update），CI success。机制修复（管家）：备份注册表健康条目从 `health-advisor` 旧树改指 `health-advisor-internal` 现行树，杜绝双树推同一远端互相覆盖。**教训：归档树保留指向同一远端的 origin 是隐患——双树共一远端，任何一边 push 都会污染另一边。**
- 同日：米桥（原米家数据桥）发布前 L2 三方会审 + 11 项整改 + 改名推送，详见商业顾问记忆 §69/§70（本会话代执行）。

### 2026-08-16

- **Hypervolt 手搓放松引导（用户需求：安卓手机用不了 Hyperice 官方 App 教程，"帮我手搓一个"）**：
  - **BLE 协议**：社区已有逆向成果（GitHub `Taxom/pyhypervolt`，Hypervolt 2/HV200 实测）——自定义服务 `31cb456a-…`，鉴权特征 `…4570`（读 4 字节挑战 → 按 MAC+查找表算 4 字节应答写回），控制特征 `…456c`（写 1 字节 0-3 调档，通知回当前档位+压力），遥测 `…456d`（电压/温度）。协议文档已存 `_tmp\hyperice-ble\pyhypervolt\docs\protocol.md`（_tmp 14 天后清理，协议要点以此条记忆为准）。
  - **成品**：`app/static/hypervolt.html`——手机浏览器单页应用，Web Bluetooth 直连筋膜枪自动调档 + 分步引导计时；连不上自动降级"手动模式"（页面提示档位，用户自己在枪上调）。流程 5 套（骑行/跑步/上肢力量/下肢力量/全身通用），每步带部位/按摩头/档位/时长/要领；内容按 §1 规矩（Hyperice 官方指引每肌群 30s–2min、避开骨骼关节；ACSM 冷身；Mayo 恢复），并内置"右膝康复期避开右膝"提醒。JS 鉴权算法与 Python 参考实现 50 组随机向量交叉验证一致。
  - **手机访问前提（一次性）**：Chrome `chrome://flags/#unsafely-treat-insecure-origin-as-secure` 填入 `http://192.168.18.104:8000` 并 Enabled（Web Bluetooth 要求安全上下文，局域网 HTTP 需此例外）；页面地址 `http://192.168.18.104:8000/static/hypervolt.html`（/static 免令牌；MAC 首连时填一次存 localStorage）。
  - **每日提醒机制（用户原始需求"每天运动后告诉我怎么放松"）**：`scripts/post_workout_recovery.py` 查 health.db 今日 Strava 活动，有运动→写 `output/recovery/<日期>_放松指导.md` + Windows 气泡通知（`scripts/show_toast.ps1`，ASCII-only ps1 无 BOM 坑）；休息日静默。计划任务 `HealthAssistantPostWorkoutRelax` 每天 21:55（21:40 同步之后；避开通勤时段），安装脚本 `scripts/install_recovery_task.ps1`，审计 XML 已存 `workspace_admin\scheduled-task-audits\HealthAssistantPostWorkoutRelax.after.2026-08-16.xml`，已注册进 `catch-up-scheduled-tasks.ps1` watch 清单。
  - **当日实测**（工作规则：当天事当天毕）：pytest 77 passed；TestClient GET /static/hypervolt.html 200；脚本真跑 exit 0（今日无活动→正确静默）；合成数据 build_guide 输出校验通过；气泡通知实测弹出。
  - **真机联调（同日完成）**：用户机型 = **Hypervolt 2 Pro**，MAC `E2:1D:AE:06:02:AF`，固件 1.15.0 / 硬件 2.4.0。PC 端 bleak 实测：鉴权一次通过；写 1/2/3 档设备通知逐一确认；**4/5 档在 2 Pro 上同样生效**（库默认校验 0-3，直接写原始字节 04/05 设备回报 4/5）；停止写 00 正常；遥测电压 16.33V、温度 33℃ 可读。页面已预填该 MAC（localStorage 可覆盖），副标题标注"2 Pro 实测 1-5 档可控"。注意：pyhypervolt 库的 set_speed 只接受 0-3，测 4/5 需直接 write_gatt_char 原始字节（脚本 `_tmp\hyperice-ble\live_test2.py`）。
  - **视频下载收尾（同日完成）**：YouTube 直连/代理均不可达（飞鸟 core 启动但隧道未建立），改走 B 站官方账号「HYPERICE海博艾斯」（mid 519439096）。坑：yt-dlp 的 B 站 extractor 被 412 风控（buvid cookie、代理均无效），但裸 API（`x/player/pagelist` + `x/player/playurl?type=mp4`，带 UA/Referer 头）正常——手写下载器 `_tmp\bili_dl.py` 绕过。成果：`hyperice\videos\` 31 个分 P 约 1GB（《Hypervolt筋膜枪使用教程》14P + 《震动轴&筋膜球使用教程 NASM》17P），全部 ffprobe 校验可播放（720p h264/aac）；目录已入 .gitignore，清单见 `hyperice\README.md`。

### 2026-08-10

- **右膝复评（08-03 处置的 7 天节点）**：用户报告右膝自 08-02 长骑（151km）后至今 8 天仍隐隐作痛、走路即痛——已触发 08-03 设定的就医红线"超过约 1 周不缓解 → 看医生"。处置：建议尽快挂骨科/运动医学科面诊（查体+必要时影像），就诊前继续免下肢训练、减少不必要步行、避免楼梯深蹲跪姿；问诊要点已给用户（痛的位置/肿胀/卡住打软/夜间痛）。**同日补充自评**：躺着/静息不痛，仅走路时轻度疼痛——无静息痛是好信号（指向负荷相关的劳损类问题，不支持严重结构损伤），但"走路仍痛"未达 08-03 复评标准，维持就诊建议（非紧急，本周内门诊即可），继续免下肢训练、减少步行。引擎侧：在膝痛解除前，每日建议不应包含任何下肢动作；08-03 起"走路无痛才恢复轻活动"标准维持。待复评：就诊结论回来后记入本节，再定恢复方案。

### 2026-08-03

- **昨日（08-02）运动数据分析（用户问"昨天运动数据分析了吗"）**：昨日 Strava "Lunch Ride" 11:44 起骑，moving 380 分钟、151.0 km、爬升 1009 m、均心 141.5/最大 169；小米当日 21132 步。引擎口径：近2天 380 分钟、急性负荷 485 vs 近4周周均 276（负荷比 1.75），readiness=恢复不足（置信度中），触发 07-27 短期过载检测。睡眠：最近一晚 6.0h，近7晚平均仅 5.1h，负债 13.1h。晨重 73.85kg/27.3%（比上次 +0.4kg，长骑后体液波动，近7次均值仍降 0.29kg）。
- **右膝走路痛+精神不佳处置（用户新症状，人脑覆盖引擎）**：引擎按数据给"低冲击护膝恢复训练 25–35 分钟"，但用户报告右膝走路疼痛——按 §1.4 疼痛即停原则改判**今日完全休息**：不做任何下肢动作（含恢复模板和晨醒15分钟流程中的下肢部分）；按 Mayo 膝痛自我处理：减少步行/避免楼梯深蹲跪姿、冰敷 15–20 分钟、抬高，观察 2–3 天。就医红线已告知：明显肿胀、无法承重、卡住/打软腿、静息或夜间痛、超过约1周不缓解 → 看医生。精神不佳归因：睡眠负债 13.1h + 昨日 380 分钟大负荷，今日核心任务是补觉，嘱今晚 23:00 前上床（本周实验即提前入睡）。明日复评：走路无痛才可恢复上肢/呼吸类轻活动。

### 2026-08-01

- **早晨居家运动方案交付（用户需求："每天早上想运动下，在家里面做"）**：按 §1 固定要求设计"晨醒 15 分钟"流程，全部动作带 AAOS/Mayo 出处与详细要领，低冲击护膝：①腹式深呼吸（Mayo 放松技术）②原地踏步热身（Mayo）③颈部侧倾拉伸（Mayo 基础拉伸）④AAOS 钟摆摆动（肩）⑤臀桥（AAOS 膝盖调理）⑥鸟狗式（Mayo 核心库）⑦跪姿平板支撑（Mayo 改良版）⑧扶椅半蹲·浅蹲重心脚跟（AAOS）⑨提踵（AAOS）⑩站姿腘绳肌/股四头肌/扶墙小腿拉伸（AAOS/Mayo）。强调疼痛即停、微喘能说话强度（Mayo 说话测试）、空腹先喝水。方案属建议内容、未改代码，无需 pytest；若后续用户要求固化进 app 模板（如新增 morning 模板），须过 test_workout_authority.py 门禁。

### 2026-07-27

- **修复"昨日414分钟运动不影响今日建议"（用户反馈驱动）**：根因有二——①负荷只按"近7天vs近4周周均"判断，单日巨量会被摊薄，且负荷降级上限只有"一般"；②"一般"在自重日（周一/五）给出的模板与"可训练"完全相同（都是 lower），降级不可见。修复：`build_summary` 新增最近2天短期过载检测（48h≥300分钟，或负荷≥近4周周均1.2倍且绝对负荷≥300——绝对下限防止淡季单次普通运动误报），触发则 readiness 直接"恢复不足"并把"最近2天运动X分钟"写入 reasons/decision_explanation/observations；`_build_workout` 的"一般"+自重日改用 recovery 模板（原 lower 静默开完整力量课）。training_result 新增 `last_2d_minutes`，`_integrated_coaching` 活动信号同步引用。实测：昨日374分钟骑行 → 今日 readiness 恢复不足、课表变为"低冲击护膝恢复训练"，决策说明显式引用374分钟。
- **个人规律挖掘上线（用户要求"定制化而非照搬专家建议"）**：新增 `_personal_patterns`（analytics.py），从本人历史数据挖三类规律：①入睡时间→睡眠时长（解析 sleep_summaries.raw_json 的 start_at/end_at，剔除<3h小睡/误记与白天段；他的真实结论：起床6:48±50min很固定，23点前入睡平均8.5h、23–01点6.2h、凌晨1点后5.6h，睡不够的根源是入睡晚）；②睡眠→次日心率/压力/步数（样本不足时如实说"测不出"并引导实验）；③活动量→相邻称重体重变化（他的数据：高/低活动日 +0.15 vs -0.08 kg/天，短期影响不明显→缺口关键在饮食端）。输出 custom_targets（专家基准×本人数据：几点睡=按起床时间倒推7.5h、如何运动、吃什么）+ 每周单变量实验（睡够比例<30%→提前上床实验；饮食记录<3天→记录周；数据不足→收集周），全部写入 summary.personal_patterns 并渲染到看板"你的规律"区块（index.html）。db.py 的 list_sleep/load_health_snapshot 补 SELECT raw_json。所有规律标注样本量+"相关不等于因果"。
- 实测：pytest 70 passed（64基线+6新增）；TestClient GET / 200，新区块真实渲染；真实数据跑出的本周实验为"23:15前上床"（单变量，含用户配合事项4条）。
- 【用户配合事项已当面告知】睡觉戴表、早起称重、23:15前上床（本周唯一变量）、三餐拍照记录。下周末复盘实验数据，若≥5晚睡够7小时则进入饮食定制阶段。
- **北极星目标卡上线（用户定调：科学方案打底+个人数据微调，最终体脂体重回正常）**：`_body_analysis` 新增 `stage_goal`——当前值→阶段一（体脂≤24%）→正常区间（20–24%）三段，含起点体重（最早记录）、进度百分比（钳制0–100）、剩余公斤数和按0.2–0.4kg/周温和速度的预计周数；看板指标区下方新增进度条卡片渲染。当前实测：72.8kg/26.7% → 阶段一70.2kg（再减2.6kg，约7–13周）→ 正常66.7–70.2kg，进度24%。pytest 72 passed。

### 2026-07-26

- **局域网手机访问上线（§11 缺口已解决）**：uvicorn 改绑 0.0.0.0 + `lan_token_guard` 中间件（非回环需 `?token=`/`X-LAN-Token`，回环免令牌，令牌自动持久化在 `data/lan_token.txt`）。手机看板地址：`http://192.168.18.104:8000/?token=<见 data/lan_token.txt>`。防火墙已有"Python"公用入站允许规则，无需新增。改动文件：app/config.py、app/main.py、app/static/js/dashboard.js、app/templates/mobile.html、scripts/healthboard.bat、scripts/start_health_services.py、run.ps1、.env.example。
- **xiaomi 同步正式重试**（针对 07-23/24/25 连续三晚 21:40 TimeoutError）：指数退避 + 鉴权类不重试，详见 §6 新增条目。
- 实测（当天改动当天实测）：pytest 61 passed（基线 49 + 新增 12）；curl 127.0.0.1 无令牌 200；curl 192.168.18.104 无令牌 403、错令牌 403、正确令牌 200（/ 与 /mobile 均 200，/static/ 放行）；手动触发 /api/sync/xiaomi（body 13、sleep 26、daily_metrics 13）与 /api/sync/strava（synced 296）均成功；服务已按非常驻模式停止，8000 端口释放、无残留进程。待用户确认后提交 git。
- **六模板权威化迁移完成（用户看板反馈驱动）**：用户反馈看板热身卡片只有"全身舒展 / 原地活动肩、髋、膝、踝"一行字看不懂——正是 §3 旧待办。已把 lower/upper/swim/run/ride/rest 六个模板全部迁移为 guide+source+sources 权威格式（出处均经 WebSearch/FetchURL 逐页核实真实存在：AAOS 膝盖/肩部调理方案、Mayo 核心库/改良俯卧撑/弹力带划船/水中运动/说话测试/热身冷身/基础拉伸/步行/放松技术、ACSM 强度与活动量指南）。**删除/替换的伤膝及无出处动作**：ride 热身"膝绕环"（违反 §1.3）→ 原地踏步+髋绕环+腿部摆动；深蹲→AAOS 扶椅半蹲（浅蹲、重心脚跟）；保加利亚分腿蹲→AAOS 俯卧直腿抬高；登山跑→Mayo 超人式；标准平板→Mayo 跪姿平板；俯卧撑→Mayo 墙壁/跪姿俯卧撑；反向划船（需低杠）→Mayo 弹力带俯身划船；椅上臂屈伸（肩前侧压力大）→Mayo 超人式；死虫式→Mayo 鸟狗式；猫牛式/手臂画圈→AAOS 钟摆摆动+Mayo 跪姿热身；开合跳→走路版动态热身；4-7-8 呼吸→Mayo 腹式深呼吸。swim/run/ride 时长强度处方未动。新增 tests/test_workout_authority.py 长期门禁，pytest 64 passed；服务已重启（PID 旧 25304 已 taskkill /T，8000 释放后重新拉起），curl 验证今日计划与 ride 三级别每动作均带 guide/source，页面 HTML 实际渲染出处。待用户确认后提交 git。

### 2026-07-25

- 09:22 自动同步成功（小米+Strava）；前一晚 21:40 小米同步超时失败一次，属近期反复出现的偶发超时（07-23、07-24 晚同样超时，重试/次日晨同步可自愈），暂观察不处理。
- 数据快照：今早体重 73.15kg、体脂 26.2%（近7次称重均值降 0.21kg，趋势温和向好）；昨晚睡眠 6.0h，近7晚平均仅 4.5h，睡眠负债 18h，准备状态"恢复不足"；近7天无记录运动（上次骑行 07-12）。
- 引擎输出：今日恢复日——轻松骑行 35–50 分钟（RPE 3，可聊天强度，不爬坡不冲刺），fallback 完全休息；热量约 1750–1900 千卡，蛋白质 118–148g，碳水按休息日 184–258g；轮换菜单"辣炒鸡胸搭配"。
- 注意：ride 类模板（含今日恢复骑）仍是无权威出处旧版（§3 待迁移），热身里含"膝绕环"违反用户固定要求 §1.3（膝盖不做环绕旋转）——已在回复中提示用户跳过该动作；下次改 ride 模板时优先修复。
- 【管家 21:53 补记】当晚 21:40 同步 xiaomi 第三次 TimeoutError（07-23/24/25 连续三晚，strava 正常；状态文件 exit_code=1 已留痕）。已不再适用"偶发、暂观察"——下次健康会话第一优先：查 passToken 有效期与网络稳定性，给 xiaomi 同步加正式重试（指数退避 1-2 次）。数据无损失风险（次日晨 14 天窗口回溯补齐）。

## 11. 待办：用户购买 NFC 贴纸（仅剩这一项；技术侧 2026-07-26 已完成）

**唯一待办（用户侧动作，无需开发）**：购买 NTAG215 NFC 贴纸（约 ¥10–15/10 个），安卓用 NFC Tools 写入"打开 URL：手机记录页地址 `http://192.168.18.104:8000/mobile?token=<见 data/lan_token.txt>`"；iPhone 用快捷指令 NFC 自动化。贴纸到位后碰一下即打开 `/mobile` 拍照记录页，全链路（局域网绑定+令牌+拍照入库）已于 2026-07-26 实测通过。

**背景（存档）**：
- 需求原文（2026-07-25 商业顾问会话转入）：用户饮食记录坚持不下来（总忘记拍），拒绝闹钟提醒方案，选定 NFC 贴纸触发路线——吃饭时手机碰一下贴在餐桌/工位的 NFC 贴纸，直接打开手机记录页拍照。
- 接收端早已存在：`/mobile` 页支持餐食照片上传 + AI 估算（OPENAI_API_KEY 已配则可用），照片不落库只存文字分析。
- 唯一技术缺口（服务仅监听 `127.0.0.1`）已解决（2026-07-26）：绑 0.0.0.0 + 局域网访问令牌（回环免令牌），隐私红线复核成立——数据仍不出内网、令牌不进 git（`data/lan_token.txt` 已 gitignore）；§8.4"仅回环监听"基线已相应修订为"回环免令牌 + 局域网令牌访问"。实测：LAN IP 无令牌 403、带令牌 200（含 /mobile），pytest 61 passed。

## 12. 2026-08-06 商业化配合任务：身高预测类产品技术边界一页纸（管家派发）

- **来源**：用户转发抖音视频"大学生做的测身高 App 月入 10 万美金 #vibecoding"（`https://v.douyin.com/2lukghtLKv8/`，数字未验证，事实核验归新媒体），指示健康顾问参与"现有 agent 变现盘点"（商业顾问记忆 §58/§59）。
- **本 agent 任务**：出一页纸技术边界评估，供商业顾问变现盘点用，写清三条：① 现有数据链（小米体脂/睡眠/心率/压力/Strava，成人自用）能支撑什么健康类产品形态；② "儿童身高预测/测量"类产品需要什么（生长曲线/遗传/骨龄等数据与模型）——我们缺什么、补什么、预测类健康声称的合规风险如实写；③ 结论：能做什么/不能做什么/最小可行形态猜想。
- **红线不变**：医疗建议边界——不输出诊断，产品形态讨论仅限技术与数据层面。

## 工作规则（2026-07-20 用户定）

- 当天事当天毕：对本项目的任务、服务、脚本做任何修复或调整，当天必须立即手动触发一次实测验证，不等下一个排定时间；验证结果记入本文件或审计目录。
- 定时任务不得安排在用户通勤时段（08:00–09:00、17:30–18:30）。
- 汇报规则（2026-07-20 用户定）：汇报结果时必须"问题+结果"成对出现——每条结果前先用一句话带上对应的需求原文（"你问：xxx → 结果：xxx"），多条需求逐条对应，禁止只说"都办好了/已修复"。

## 待你决定

- [x] **云端数据同步项目**——已决（2026-08-11 用户拍板）：**整个项目取消**。配套处置：计划任务 `HealthAssistantCloudDigestExport` 已禁用、catch-up 补跑清单与 generate-task-wrappers.py 登记已撤、生成的 VBS 包装已删；`桌面\健康顾问-云端复刻\` 文件夹与云端 Kimi 助手已建内容保留不动（如要清理由用户另行指示）。原问题（要不要上"真·全自动"）随项目取消作废。原条目：（2026-08-11，管家会话代健康顾问执行）已实现本地全自动——`scripts/export_cloud_digest.py` 每周日 21:05 自动重建健康数据摘要.md，但上传进云端 Kimi 助手这最后一程无法自动（消费版无开放接口）。可选：① 维持半自动；② Moonshot API 自建（要花钱）；③ 浏览器自动化（动账号、脆弱）。
- [x] 本项目本地仓库推送目标——已决（2026-07-29 用户授权"你自己判断吧"，商业顾问执行）：**方案①解除 health-advisor 归档**。已 unarchive（isArchived=false），17 个积压提交（含 07-27"北极星目标卡"等）全部推送上岸（`da7b7a3..cdbc8b2`），main 与远端同步。后续去留（是否按 07-25 合并决策再归档）下次健康顾问会话评估，不着急。**同日 CI 修复（商业顾问代执行）**：17 提交触发的 CI 红了，根因两条——① 时区断言（测试写死 +08:00 标签，UTC runner 差 8 小时）；② 测试依赖本地真实 data/health.db（干净检出无此文件，应用 lifespan 未进就不建库）。修复均在 ci.yml 基础设施层（env TZ=Asia/Shanghai + pytest 前 `init_db()` 建空库），未动应用与测试代码；本地 72 项测试全绿，推送 `3b6dc4d` 后 CI 复跑 success。遗留：测试写死时区、TestClient 未走 lifespan 属测试设计债，建议健康顾问后续把测试改为时区无关 + 合成库 fixture。原条目：本地 `projects\health-advisor-internal` 的 remote 仍指向**已归档**的 `shkyyy18/health-advisor`，本地领先 16 个提交无法推送。可选：① 解除 health-advisor 归档继续用它；② 迁推 health-assistant；③ 另定新仓。

- 2026-08-11（三方会审发现，待健康顾问会话整改）：① 【高】health-advisor 目录不是干净兼容壳——Codex 查证它是带独立 .git、与 health-advisor-internal 已分叉的完整旧工作树，且未在 workspace.json 登记；建议定性为只读归档并禁止开发，或在 AGENTS.md 首行声明跳转；② 【高】红线口径需澄清："健康数据零外发"与既有功能冲突——餐食照片走 OpenAI API、云端摘要上传 Kimi（均为用户知情功能），建议明确"哪类数据可在知情授权+脱敏+最小化下外发"，否则绝对表述与实况脱节；③ 记忆 §6 的 08:10 同步时间已过时（实盘 07:30+21:40，已避开通勤禁区，两家评审均确认）；④ §7"已取消一刀切 UTF-8"与全局第 4 条矛盾，改"按全局 §3.4 执行"；⑤ §11 待办标题与正文脱节（仅剩用户购 NFC）；⑥ 0.0.0.0 局域网绑定例外已由管家补登记留档（audit\2026-08-11-健康服务局域网例外留档.md，当前服务未运行无实际暴露），全局规则例外条文列入批量确认候选。
  → **已办结（2026-08-13 管家会话代执行）**：① health-advisor 已定性为归档只读——其 AGENTS.md 首行加醒目归档声明（禁止开发、指向 health-advisor-internal），并已在 workspace.json 登记（status: archived-readonly）；② §8.2 隐私红线改为明确口径"原始健康数据（SQLite、完整导出、原始照片）零外发；仅在用户知情授权的功能内按脱敏+最小化原则外发必要内容（当前实例：餐食照片→OpenAI 分析、健康数据摘要→云端 Kimi 助手）"，§8.4 验收指标隐私行同步修订；③ §6 及 §8.4（验收指标+基线记分卡）三处同步时间 08:10 已按实盘改为 07:30+21:40；④ §7 改为"文本编码按全局 §3.4（文本统一 UTF-8）执行"；⑤ §11 标题与正文已改一致，如实反映仅剩"用户购买 NFC 贴纸"一项待办，技术侧完成内容归入背景存档；⑥ 留档已存在，本条无需操作；全局规则例外条文属用户拍板批量确认候选，未动全局规则文件。

- 2026-08-13（管家核实）：HealthAssistantDailySync 07:30 场与 catch-up 补跑场均 exit 1——**根因=Windows 系统代理假死**：飞鸟加速异常退出但系统代理残留开启指向死端口，httpx 读注册表代理导致全部外发请求被拒（WinError 10061），非小米/Strava 服务端波动、非登录过期。处置：管家重启飞鸟加速后手动重跑 daily_sync.py exit 0 全量成功（睡眠 17、心率 7698、Strava 300 条），health.db 已更新。完整根因链与长效建议（小米端点可 trust_env=False 直连、Strava 代理失败回退直连、meal_analysis 的 OpenAI 必须走代理不能一刀切）见 `workspace_admin\audit\2026-08-13-系统代理断链致健康同步失败.md`；代码改进留待健康顾问会话评估。另注意：该假死模式在 08-11/08-12 两场 21:40 晚间失败中疑似同样出现，若 VPN 再异常退出且代理残留，21:40 场可能复发。

- 2026-08-13（管家子代理代执行，用户拍板两项改动已落地）：**① 餐食照片分析 LLM 由 OpenAI（gpt-4o-mini）切换为 KI 中转站 gpt-5.6-sol**（`https://cdk.goodmoonlight.com/responses`，OpenAI Responses 线协议，直连无需代理；设置项改 MEAL_LLM_API_KEY/MEAL_LLM_BASE_URL/MEAL_LLM_MODEL，旧 OPENAI_* 不再读取，密钥已写入 .env 且 .env 本就在 .gitignore）；**② 同步链路去代理化**——小米桥 httpx 显式 trust_env=False 直连（mi_fitness_data_bridge `db12da8`，该仓仅此一处客户端创建点）；Strava 代理优先、ConnectError/ConnectTimeout 回退 trust_env=False 直连重试一次（本仓 `71852c1`）；餐食分析客户端 trust_env=False（本仓 `9a542da`）。验证：health-advisor-internal pytest 77 passed、mi_fitness_data_bridge pytest 38 passed、1x1 测试图真实调中转站 200 且 JSON 可解析。注意：strava 回退初版 asynccontextmanager 方案有二次 yield 缺陷（"generator didn't stop after athrow()"），已改为 _run_with_client 重试整个幂等请求体的函数式实现。§8.2 隐私红线口径中"餐食照片→OpenAI 分析"实例自此改为"餐食照片→KI 中转站分析"（本条目留痕，正文待下次整理时统一修订）。

- 2026-08-21（用户拍板，管家会话记录）：**右膝不挂号、不就诊**——就医红线（08-10 触发，超期第 11 天）挂账就此销账，早报/待决浮出不再追问。后续口径：膝痛管理回到"观察+自我管理"档（免下肢训练、减少不必要步行、避免楼梯深蹲跪姿维持不变）；若症状升级（肿胀/无法承重/卡住打软/静息或夜间痛）再由用户自行决定就医，agent 不主动催。- 2026-09-01（管家会话代登记）：**用户自述"膝盖还是不舒服"**——背景：8/3 骑行 150km 后一个月未恢复（膝不适+一吹空调就感冒，用户在手机心理咨询记录中自述）。管理口径维持 08-21 拍板"观察+自我管理"档（免下肢训练、减少不必要步行、避免楼梯深蹲跪姿）；升级条件不变（肿胀/无法承重/卡住打软/静息或夜间痛→用户自行决定就医，agent 不催）。关联背景：同记录中用户自述身体"消耗过度未恢复"，恢复优先级=电厂三机组第一台的口径，与本项目"恢复是训练的一部分"一致。另注意：该记录出自手机情感咨询备份（高敏，存元认知项目 data/情感咨询/，已 gitignore），引用时只取健康相关事实、不外传心理内容。

## 2026-09-07: Repository remediation and first-run regression

- Kept GitHub repository `shkyyy18/health-advisor-internal`; the duplicate `shkyyy18/health-advisor` repository was deleted. The pre-deletion backup is under `D:\AIWork\workspace_admin\backups\health-advisor-before-removal-20260907`.
- Fixed the first-install race: when SQLite does not exist yet, `_snapshot_time()` returns a no-sync status instead of raising `FileNotFoundError` and producing HTTP 500. Added synthetic temporary-database regression coverage.
- Aligned README, project guides, launch scripts, and package metadata with the canonical repository URL and local path. Removed accidental UTF-8 BOMs from Windows `.bat/.vbs` launchers.
- Validation: health-advisor-internal pytest 79 passed, compileall passed, and `git diff --check` passed. No real health data or credentials were used.



## 2026-09-07：health-advisor-internal 本地整改

- 当前工作目录为 `D:\AIWork\repos\health-advisor-internal`，远端项目链接统一为 `https://github.com/shkyyy18/health-advisor-internal`；不读取或修改 mi_fitness_data_bridge。
- 复核并修复两个可复现问题：首次安装时 SQLite 尚未创建，仪表盘不再因读取数据库 mtime 失败；本地 Strava callback（localhost/127.0.0.1/::1）不再被误判为公网隧道。
- 新增回归测试覆盖：已存在但从未同步的数据库仍显示“尚未同步”；本地 callback 不开启公网隧道。
- README、pyproject 项目链接/路径、release checklist 已与当前仓库和实测结果同步。
- 2026-09-07 实测：`python -m pytest -q -p no:cacheprovider` 为 82 passed、1 warning；应用与同步入口 `py_compile` 通过。wheel 命令因当前环境 `build` 模块无可执行入口未运行成功。

