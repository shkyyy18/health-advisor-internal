# 健康顾问｜项目记忆

> 本文件是"健康顾问"项目（`D:\AIWorkspace\projects\health_assistant`）的长期、可跨会话读取的本地记忆入口。

更新日期：2026-07-25（Asia/Shanghai）

## 1. 用户的固定要求（训练建议）

1. **动作必须有权威出处**：训练动作只采用权威机构（如 AAOS 美国骨科医师学会、Mayo Clinic 梅奥诊所、ACSM 美国运动医学会等）推荐的方案，并在输出中标注出处；不接受无来源的自编动作组合。
2. **每个动作必须带详细解释**：用户是健身新手，每个动作要写清具体怎么做、发力部位、要领和常见错误（例如"手臂画圈"要解释以肩为轴画圈，"死虫式"要解释起始姿势）。
3. **不伤身体，尤其膝盖压力小**：优先低冲击、膝盖不负重不屈曲或浅幅度的动作；膝盖是铰链关节，不做膝环绕类旋转动作；深蹲类一律浅蹲、扶椅、重心在脚跟。
4. **疼痛即停**：沿用 AAOS 原则——运动中不应感到疼痛，任何动作引起疼痛立即停止；有确诊伤病先由医生确认动作。

## 2. 已完成：恢复日模板权威化（2026-07-20）

- `app/analytics.py` 的 `_WORKOUT_LIBRARY["bodyweight"]["recovery"]` 已替换为"低冲击护膝恢复训练"：仰卧直腿抬高、臀桥、侧卧髋外展、扶椅半蹲、站姿腘绳肌弯举、提踵、跪姿平板支撑、鸟狗式 + 三个 AAOS 拉伸。
- 每个动作带 `guide`（详细做法+要领）和 `source`（AAOS / Mayo Clinic）字段；模板带 `sources` 出处列表；`_build_workout` 返回值和 `app/templates/index.html` 已渲染这三项。
- 替换要点：删膝环绕；靠墙静蹲 → 仰卧直腿抬高 + 扶椅半蹲；标准平板 → Mayo 跪姿版；死虫式 → 鸟狗式（Mayo 官方核心库）。
- 42 个测试通过，服务已重启并验证线上生效。

## 3. 已完成：全部训练模板权威化（2026-07-26 收尾）

`_WORKOUT_LIBRARY` 全部模板均已迁移为"guide 详解 + source 出处 + 模板级 sources 列表"格式（recovery 于 2026-07-20 完成，其余六个于 2026-07-26 完成，详见 §10 当日日志）。新增 tests/test_workout_authority.py 作为长期门禁：每个动作必须带 ≥60 字 guide 和 AAOS/Mayo/ACSM 出处、每个模板必须带 sources、处方字段（name/detail/note）禁止出现膝绕环类动作、swim/run/ride 时长处方不得擅改。今后改动任何模板必须通过该测试。

## 4. 服务运维备注（2026-07-20 改为"非常驻"模式）

- 本地服务：`http://127.0.0.1:8000`，uvicorn 无热重载，改代码后需重启生效。
- **绑定方式（2026-07-26 起）**：uvicorn 绑 `0.0.0.0`（手机局域网可达），`app/main.py` 的 `lan_token_guard` 中间件要求**非回环来源**必须带访问令牌（`?token=…` 查询参数或 `X-LAN-Token` 头，否则 403）；**回环（127.0.0.1/::1）免令牌**，daily_sync.py、open_dashboard.py 等本地脚本不受影响。`/static/` 静态资源放行（只是界面文件，无健康数据），页面与 API 全部保护。令牌来源：环境变量 `HEALTH_LAN_TOKEN`；缺省自动生成并持久化在 `data/lan_token.txt`（data/ 已 gitignore，重启不变，手机书签持续有效）。`dashboard.js` 与 `mobile.html` 自动把 URL 里的 token 拼到所有 /api 请求（含心跳）和站内链接。防火墙：本机已有"Python"公用配置文件 TCP 入站允许规则（WLAN 即公用），无需新增；如换机器不通，管理员执行 `netsh advfirewall firewall add rule name="Health Assistant 8000" dir=in action=allow protocol=TCP localport=8000`。
- **用户要求后端不常驻**：登录自启动任务 `HealthAssistant` 已于 2026-07-20 禁用（`Disable-ScheduledTask`，恢复用 `Enable-ScheduledTask -TaskName HealthAssistant`）。现在只有两种运行时机：①每日同步任务临时拉起、同步完自动停止；②用户点书签手动启动，**关闭看板标签页后约 5 分钟无心跳自动退出**（心跳机制见下）。
- 手动入口：浏览器书签 `healthboard://`（2026-07-20 注册的自定义协议，注册表 `HKCU\Software\Classes\healthboard` → `wscript.exe scripts\run_healthboard.vbs` → 隐藏运行 `scripts\healthboard.bat`，删除该键即卸载协议）。桌面 bat 已按用户要求删除。**全程无任何命令行窗口**。
- 心跳自动退出（2026-07-20）：`dashboard.js` 每 30 秒 POST `/api/heartbeat`；`app/main.py` 的 `_idle_watchdog` 每 30 秒检查，超过 `HEALTH_IDLE_TIMEOUT_SECONDS`（默认 300 秒）无心跳就 `os._exit(0)`。每日同步一两分钟内结束并自行停止，不受看门狗影响。
- `scripts\healthboard.bat` 逻辑：`pythonw scripts\open_dashboard.py`（等端口就绪 → **先采集小米 + Strava 最新数据 → 采完才打开看板页**，日志在 `logs/open_dashboard.log`），若 8000 已在监听则只同步+开页面、不重复启动 uvicorn（此时点书签等于一次全量手动刷新）；uvicorn 输出追加到 `logs/app.out.log` / `app.err.log`。**注意：bat 内容必须纯 ASCII**——UTF-8 中文 title 行会被 cmd 按 GBK 断行解析，吞掉下一行 `cd /d`（2026-07-20 实录，协议唤起时工作目录错误导致 uvicorn 起不来）；`open_dashboard.py` 的 print 必须带 try/except（pythonw 无控制台）。
- 旧进程可能是 multiprocessing 子进程（spawn_main），杀父进程后要确认子进程退出、8000 端口确实释放，再启动新进程；`daily_sync.py` 的 `stop_service()` 用 `taskkill /T` 连带杀子进程。

## 5. 2026-07-20 代码审查 10 项 bug 修复

- 修复位置：D:\AIWorkspace\projects\health_assistant（app/analytics.py、app/main.py、app/static/js/dashboard.js）。
- 要点：_build_workout 休息/恢复日 intensity 统一为"恢复"；save_quick_meal 对 naive eaten_at 按本地时区入库；_iso_to_local_date 真正转本地时区；_body_analysis 周口径统一系统本地时区；豆腐鸡蛋 variant density 0.10、minimum 300；latest_steps 为 NULL 时返回 None；饮食调整分支 -0.15 边界消除区间空洞并按 trend_sufficient 区分文案；dashboard.js 用餐提醒用本地日期；Strava OAuth/同步捕获 httpx.HTTPError 转 502。
- 回归测试：tests/test_bugfix_regressions.py（问题1/2/5 共4条）；test_low_sleep_reduces_intensity 断言随修复由"低强度耐力"改为"恢复"。pytest 46 passed。

## 6. 每日自动同步（用户要求：数据必须每天自动同步）

- 用户明确要求 Strava 和小米数据每天自动同步，不接受手动刷新。
- 实现：`scripts/daily_sync.py` 通过本地服务端点（`/api/sync/xiaomi`、`/api/sync/strava`）同步，服务不在时先拉起、**同步完后自动停止**（2026-07-20 起，配合"后端不常驻"要求）；日志写 `logs/daily_sync.log`，有失败项退出码为 1。
- 部署：Windows 计划任务 `HealthAssistantDailySync`，每天 08:10（收睡眠+晨重）和 21:40（收白天活动和步数）运行，`StartWhenAvailable` 补跑错过的任务；安装/卸载用 `scripts/install_sync_task.ps1`。
- 任务审计 XML 存于 `D:\AIWorkspace\workspace_admin\scheduled-task-audits\HealthAssistantDailySync-{before,after}.*`。
- 小米 passToken：无官方有效期，社区经验几个月到一年；上次扫码 2026-07-14，截至 2026-07-20 每日同步均成功。**注意**：2026-07-20 前 `xiaomi_sync.py` 把任何连接失败（含网络波动）都误报为"passToken 可能已过期"；现已修复——连接失败重试 3 次，并按 `adapter.last_error` 区分：网络类错误提示"无需重新扫码"，只有鉴权类错误才提示重新 `python scripts/mijia_health_sync.py login` 扫码。另外小米云偶发采集中途失败（空原因的 "Mi Fitness request failed: "，2026-07-20 实录，重试即成功），`_collect_all` 已加整轮重试。Strava webhook/ngrok 当前按配置关闭（HEALTH_ENABLE_NGROK 未开启），实时推送停用，但每日两次轮询同步不受影响。
- **正式重试（2026-07-26，针对 07-23/24/25 连续三晚 21:40 场 TimeoutError）**：①`xiaomi_sync.py` 连接与采集重试改为指数退避（3s、6s），鉴权类错误（401/Unauthorized/passToken 等标记）不再重试、直接提示重新扫码；②`scripts/daily_sync.py` 对网络类失败（TimeoutError/URLError/OSError）指数退避重试 2 次（30s、60s），HTTP/鉴权类错误不重试；③`/api/sync/xiaomi` 端点加 asyncio 锁串行化，客户端超时重试时不会与仍在执行的上一轮并发写库（upsert 幂等）。测试：tests/test_lan_access.py。

## 7. 文本编码规则（2026-07-20）

- 工作区已取消"一刀切 UTF-8"公共规则，本节按本项目实际技术栈约定：Python 读写文本文件显式 `encoding="utf-8"`（日志、文本导出含中文）；健康数据存 SQLite，原生存文本无需额外处理。
- PowerShell 5.1 脚本（`run.ps1`、`scripts/*.ps1`）显式 `-Encoding UTF8`，`.ps1` 必须保存为 UTF-8 带 BOM——无 BOM 时 PS 5.1 按 GBK 解析中文导致脚本断裂（2026-07-20 实录）。
- 与 `projects/health_assistant/AGENTS.md` 的 "Text encoding" 小节保持一致。

## 8. 外部升级建议的评审规则（2026-07-20 建立）

闭环契约见 `workspace_admin\docs\UPGRADE_SUGGESTION_LOOP.md`（调度持有）；本项目目录名 `health_advisor`，建议与反馈文件在 `output\upgrade_suggestions\health_advisor\`。

### 8.1 建议分类与验证手段

| 类别 | 例子 | 验证手段 |
|---|---|---|
| 训练内容/模板 | 新动作、模板调整、饮食建议 | 权威出处核验（AAOS/Mayo/ACSM 等，§1）+ 不伤膝/疼痛即停原则逐条核对 + pytest 回归 |
| 数据/同步 | 新数据源、同步逻辑、指标口径 | 同步成功率与数据完整性核验；不破坏"每日自动同步"（§6） |
| 工具链/看板 | 看板功能、脚本、心跳机制 | pytest 通过；回环免令牌 + 局域网令牌访问（§4）；非常驻模式（§4 心跳看门狗）不回归 |
| 运维 | 计划任务、部署方式 | 可回滚；`StartWhenAvailable` 补跑；任务审计 XML 留档惯例（§6） |

### 8.2 判定门槛（任一不过即拒绝或暂缓）

1. **隐私红线（一票否决）**：健康数据不出本机、不进 git、不外发任何第三方服务。
2. **医学安全性（一票否决）**：无权威出处的训练/健康建议直接 REJECT；违反不伤膝、疼痛即停原则的直接 REJECT。
3. **可验证**：有 §8.1 对应类别的可执行验证手段。
4. **单变量**：能落实为一次只改一处的具体改动；笼统建议打回。
5. **兼容用户固定要求**：不得破坏 §1（出处+详解）、§4（后端非常驻）、§6（每日自动同步）。

### 8.3 判定档位与原因码

候选（CANDIDATE）/ 留存观察（HOLD，登记前置条件）/ 拒绝（REJECT，附原因码）：`VIOLATES_PRIVACY` / `NO_AUTHORITATIVE_SOURCE` / `UNSAFE_FOR_KNEE` / `UNTESTABLE` / `TOO_VAGUE` / `CONFLICTS_USER_REQUIREMENTS` / `DUPLICATE:<条目>` / `LOW_CREDIBILITY`。

### 8.4 目标声明与基线记分卡（2026-07-20）

**目标声明**（三层结构按契约 §3）：

- **结果目标（北极星）**：让用户在**零伤病**前提下身体更健康——体能、活动量、睡眠趋势长期向好。健康是慢变量、多因变量，任何"快"字头的优化（快速减重、突击训练）一律视为偏离北极星。
- **验收指标（通过线）**：

| 维度 | 目标（通过线） |
|---|---|
| 训练内容 | 100% 动作带权威出处（AAOS/Mayo/ACSM 等）与详细要领；零违反不伤膝/疼痛即停原则 |
| 数据同步 | 每日 08:10/21:40 两次自动同步成功率 100%；失败可自动恢复 |
| 工具链 | pytest 全通过；回环免令牌 + 局域网令牌访问（2026-07-26 起，§4） |
| 服务形态 | 非常驻模式不回归：无心跳 5 分钟自动退出，无残留进程 |
| 隐私 | 健康数据零外发、零入 git（一票否决项，不可 trade-off） |

- **红线约束**：不伤膝/疼痛即停原则不可 trade-off；任何指标优化不得以牺牲医学安全性或隐私为代价。

**基线记分卡**：

| 维度 | 基线 | 改进怎么度量 |
|---|---|---|
| 数据同步 | 每日 08:10/21:40 两次自动同步，近期成功率 100% | 成功率、失败恢复时长（观察 2 周） |
| 内容权威化 | 全部 7 个模板（recovery + 六模板）已权威化（2026-07-26），test_workout_authority.py 长期门禁 | 权威化覆盖率 100%，门禁不回归 |
| 工具链 | pytest 70 passed（2026-07-27） | 门禁不回归 + 新能力有测试证据 |
| 服务形态 | 非常驻 + 心跳 5 分钟自动退出；书签手动唤起可用 | 无残留进程、唤起成功率 |

判定公式与回退义务按契约 §4：目标维度可度量改进 + 其他维度无回归；观察期内拿不出证据的升级回退。采纳后更新本表基线。

## 9. 已收尾：并行会话遗留文件已提交（2026-07-25）

2026-07-25 健康顾问会话按本节要求完成收尾：提交前核对 diff 与本文件 §4/§6 描述一致，pytest 49 passed，一次性提交（commit 97d7a51，7 个文件，含 `scripts/healthboard.bat`、`scripts/run_healthboard.vbs`）。此前正文（2026-07-20 晚调度会话检查结论）：pytest 49 passed；21:40 计划同步 xiaomi 报 `TimeoutError: timed out` 失败一次，21:47 重跑全部成功，非常驻模式正常。

## 10. 今日更新日志

### 2026-07-27

- **修复"昨日414分钟运动不影响今日建议"（用户反馈驱动）**：根因有二——①负荷只按"近7天vs近4周周均"判断，单日巨量会被摊薄，且负荷降级上限只有"一般"；②"一般"在自重日（周一/五）给出的模板与"可训练"完全相同（都是 lower），降级不可见。修复：`build_summary` 新增最近2天短期过载检测（48h≥300分钟，或负荷≥近4周周均1.2倍且绝对负荷≥300——绝对下限防止淡季单次普通运动误报），触发则 readiness 直接"恢复不足"并把"最近2天运动X分钟"写入 reasons/decision_explanation/observations；`_build_workout` 的"一般"+自重日改用 recovery 模板（原 lower 静默开完整力量课）。training_result 新增 `last_2d_minutes`，`_integrated_coaching` 活动信号同步引用。实测：昨日374分钟骑行 → 今日 readiness 恢复不足、课表变为"低冲击护膝恢复训练"，决策说明显式引用374分钟。
- **个人规律挖掘上线（用户要求"定制化而非照搬专家建议"）**：新增 `_personal_patterns`（analytics.py），从本人历史数据挖三类规律：①入睡时间→睡眠时长（解析 sleep_summaries.raw_json 的 start_at/end_at，剔除<3h小睡/误记与白天段；他的真实结论：起床6:48±50min很固定，23点前入睡平均8.5h、23–01点6.2h、凌晨1点后5.6h，睡不够的根源是入睡晚）；②睡眠→次日心率/压力/步数（样本不足时如实说"测不出"并引导实验）；③活动量→相邻称重体重变化（他的数据：高/低活动日 +0.15 vs -0.08 kg/天，短期影响不明显→缺口关键在饮食端）。输出 custom_targets（专家基准×本人数据：几点睡=按起床时间倒推7.5h、如何运动、吃什么）+ 每周单变量实验（睡够比例<30%→提前上床实验；饮食记录<3天→记录周；数据不足→收集周），全部写入 summary.personal_patterns 并渲染到看板"你的规律"区块（index.html）。db.py 的 list_sleep/load_health_snapshot 补 SELECT raw_json。所有规律标注样本量+"相关不等于因果"。
- 实测：pytest 70 passed（64基线+6新增）；TestClient GET / 200，新区块真实渲染；真实数据跑出的本周实验为"23:15前上床"（单变量，含用户配合事项4条）。
- 【用户配合事项已当面告知】睡觉戴表、早起称重、23:15前上床（本周唯一变量）、三餐拍照记录。下周末复盘实验数据，若≥5晚睡够7小时则进入饮食定制阶段。

### 2026-07-26

- **局域网手机访问上线（§11 缺口已解决）**：uvicorn 改绑 0.0.0.0 + `lan_token_guard` 中间件（非回环需 `?token=`/`X-LAN-Token`，回环免令牌，令牌自动持久化在 `data/lan_token.txt`）。手机看板地址：`http://192.168.18.104:8000/?token=<见 data/lan_token.txt>`。防火墙已有"Python"公用入站允许规则，无需新增。改动文件：app/config.py、app/main.py、app/static/js/dashboard.js、app/templates/mobile.html、scripts/healthboard.bat、scripts/start_health_services.py、run.ps1、.env.example。
- **xiaomi 同步正式重试**（针对 07-23/24/25 连续三晚 21:40 TimeoutError）：指数退避 + 鉴权类不重试，详见 §6 新增条目。
- 实测（当天改动当天实测）：pytest 61 passed（基线 49 + 新增 12）；curl 127.0.0.1 无令牌 200；curl 192.168.18.104 无令牌 403、错令牌 403、正确令牌 200（/ 与 /mobile 均 200，/static/ 放行）；手动触发 /api/sync/xiaomi（body 13、sleep 26、daily_metrics 13）与 /api/sync/strava（synced 296）均成功；服务已按非常驻模式停止，8000 端口释放、无残留进程。待用户确认后提交 git。
- **六模板权威化迁移完成（用户看板反馈驱动）**：用户反馈看板热身卡片只有"全身舒展 / 原地活动肩、髋、膝、踝"一行字看不懂——正是 §3 旧待办。已把 lower/upper/swim/run/ride/rest 六个模板全部迁移为 guide+source+sources 权威格式（出处均经 WebSearch/FetchURL 逐页核实真实存在：AAOS 膝盖/肩部调理方案、Mayo 核心库/改良俯卧撑/弹力带划船/水中运动/说话测试/热身冷身/基础拉伸/步行/放松技术、ACSM 强度与活动量指南）。**删除/替换的伤膝及无出处动作**：ride 热身"膝绕环"（违反 §1.3）→ 原地踏步+髋绕环+腿部摆动；深蹲→AAOS 扶椅半蹲（浅蹲、重心脚跟）；保加利亚分腿蹲→AAOS 俯卧直腿抬高；登山跑→Mayo 超人式；标准平板→Mayo 跪姿平板；俯卧撑→Mayo 墙壁/跪姿俯卧撑；反向划船（需低杠）→Mayo 弹力带俯身划船；椅上臂屈伸（肩前侧压力大）→Mayo 超人式；死虫式→Mayo 鸟狗式；猫牛式/手臂画圈→AAOS 钟摆摆动+Mayo 跪姿热身；开合跳→走路版动态热身；4-7-8 呼吸→Mayo 腹式深呼吸。swim/run/ride 时长强度处方未动。新增 tests/test_workout_authority.py 长期门禁，pytest 64 passed；服务已重启（PID 旧 25304 已 taskkill /T，8000 释放后重新拉起），curl 验证今日计划与 ride 三级别每动作均带 guide/source，页面 HTML 实际渲染出处。待用户确认后提交 git。

### 2026-07-25

- 09:22 自动同步成功（小米+Strava）；前一晚 21:40 小米同步超时失败一次，属近期反复出现的偶发超时（07-23、07-24 晚同样超时，重试/次日晨同步可自愈），暂观察不处理。
- 数据快照：今早体重 73.15kg、体脂 26.2%（近7次称重均值降 0.21kg，趋势温和向好）；昨晚睡眠 6.0h，近7晚平均仅 4.5h，睡眠负债 18h，准备状态"恢复不足"；近7天无记录运动（上次骑行 07-12）。
- 引擎输出：今日恢复日——轻松骑行 35–50 分钟（RPE 3，可聊天强度，不爬坡不冲刺），fallback 完全休息；热量约 1750–1900 千卡，蛋白质 118–148g，碳水按休息日 184–258g；轮换菜单"辣炒鸡胸搭配"。
- 注意：ride 类模板（含今日恢复骑）仍是无权威出处旧版（§3 待迁移），热身里含"膝绕环"违反用户固定要求 §1.3（膝盖不做环绕旋转）——已在回复中提示用户跳过该动作；下次改 ride 模板时优先修复。
- 【管家 21:53 补记】当晚 21:40 同步 xiaomi 第三次 TimeoutError（07-23/24/25 连续三晚，strava 正常；状态文件 exit_code=1 已留痕）。已不再适用"偶发、暂观察"——下次健康会话第一优先：查 passToken 有效期与网络稳定性，给 xiaomi 同步加正式重试（指数退避 1-2 次）。数据无损失风险（次日晨 14 天窗口回溯补齐）。

## 11. 待办：NFC 贴纸一键触发手机饮食拍照记录（2026-07-25 商业顾问会话转入）

**需求原文**：用户饮食记录坚持不下来（总忘记拍），拒绝闹钟提醒方案，选定 NFC 贴纸触发路线——吃饭时手机碰一下贴在餐桌/工位的 NFC 贴纸，直接打开手机记录页拍照。

**现状盘点**：
- 接收端已存在：`/mobile` 页支持餐食照片上传 + AI 估算（OPENAI_API_KEY 已配则可用），照片不落库只存文字分析。
- ~~**唯一技术缺口**：服务仅监听 `127.0.0.1`~~ **已解决（2026-07-26）**：按候选方案①实施——绑 0.0.0.0 + 局域网访问令牌（回环免令牌），隐私红线复核成立：数据仍不出内网、令牌不进 git（`data/lan_token.txt` 已 gitignore）；§8.4"仅回环监听"基线已相应修订为"回环免令牌 + 局域网令牌访问"。实测：LAN IP 无令牌 403、带令牌 200（含 /mobile），pytest 61 passed。手机碰 NFC 贴纸要打开的地址：`http://192.168.18.104:8000/mobile?token=<见 data/lan_token.txt>`。
- **用户侧动作（无需开发）**：购买 NTAG215 NFC 贴纸（约 ¥10–15/10 个），安卓用 NFC Tools 写入"打开 URL：手机记录页地址"；iPhone 用快捷指令 NFC 自动化。

**实施建议**：先做最小改动（局域网绑定+令牌），改完跑 pytest 并实测手机碰贴纸→拍照→入库全链路；验证结果记入本节。完成后更新 §8.4 基线记分卡。

## 工作规则（2026-07-20 用户定）

- 当天事当天毕：对本项目的任务、服务、脚本做任何修复或调整，当天必须立即手动触发一次实测验证，不等下一个排定时间；验证结果记入本文件或审计目录。
- 定时任务不得安排在用户通勤时段（08:00–09:00、17:30–18:30）。
- 汇报规则（2026-07-20 用户定）：汇报结果时必须"问题+结果"成对出现——每条结果前先用一句话带上对应的需求原文（"你问：xxx → 结果：xxx"），多条需求逐条对应，禁止只说"都办好了/已修复"。
